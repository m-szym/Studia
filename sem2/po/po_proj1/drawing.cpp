#include"drawing.h"

void draw_symbol_at_xy(const char symbol, const XY location)
{
	//gotoxy(map_location.x, map_location.y);
	//putch(symbol);
}

void draw_symbol_at_xy(const char symbol, const int x, const int y)
{
	//gotoxy(x, y);
	//putch(symbol);
}
